# vbijalwan.github.io

This is a simple static portfolio website built for GitHub Pages.

## How to publish
1. Create a repo named `vbijalwan.github.io` on GitHub (you already did).
2. Upload these files to the repository root or use git to push them.
3. In the repo settings → Pages, ensure branch: `main` and folder: `/ (root)`.
4. Visit: https://vbijalwan.github.io/
